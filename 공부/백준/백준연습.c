#include <stdio.h>

int main()
{
    int n, ans, tmp;
    int i;

    scanf("%d", &n);

    while (1)
    {
        tmp = n - 5;
        if (tmp % 3 == 0)
        {
            printf("%d")
        }
        
    }

    return 0;
}